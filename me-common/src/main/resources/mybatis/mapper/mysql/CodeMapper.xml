<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.me.inner.mapper.CodeMapper">

    <select id="listType" resultType="java.lang.String">
        SELECT DISTINCT type_x FROM ${tablePrefix}code WHERE active_c = 'A'
    </select>

    <select id="listCodeByType" resultType="com.me.inner.dto.CodeDTO">
        SELECT
            codeId_n AS codeId,
            type_x AS type,
            name_x AS name,
            value_x AS value,
            desc_x AS description,
            active_c AS active,
            crtBy_m AS createBy,
            crtOn_dt AS createDate
        FROM ${tablePrefix}code
        WHERE active_c = 'A'
        <if test="type!=null and type!=''">
            AND type_x = #{type}
        </if>
        LIMIT #{pagination.begin}, #{pagination.limit}
    </select>
    
    <select id="getCodeByTypeAndName" resultType="com.me.inner.dto.CodeDTO">
        SELECT
            codeId_n AS codeId,
            type_x AS type,
            name_x AS name,
            value_x AS value,
            desc_x AS description,
            active_c AS active,
            crtBy_m AS createBy,
            crtOn_dt AS createDate
        FROM ${tablePrefix}code WHERE type_x = #{type} AND name_x = #{name} AND active_c = 'A'
    </select>

    <select id="saveCode" parameterType="com.me.inner.dto.CodeDTO">
        INSERT INTO ${tablePrefix}code
        (
            type_x,
            name_x,
            value_x,
            desc_x,
            active_c,
            crtBy_m,
            crtOn_dt
        )
        VALUES
        (
            #{type, jdbcType=VARCHAR},
            #{name, jdbcType=VARCHAR},
            #{value, jdbcType=VARCHAR},
            #{description, jdbcType=VARCHAR},
            #{active, jdbcType=CHAR},
            #{createBy, jdbcType=VARCHAR},
            #{createDate, jdbcType=TIMESTAMP}
        )
    </select>

    <update id="deleteCode" parameterType="java.lang.Integer">
        UPDATE ${tablePrefix}code SET active_c = 'I' WHERE codeId_n = #{codeId}
    </update>

</mapper>